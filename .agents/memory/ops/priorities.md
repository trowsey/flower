# Ops priorities

1. Run token-auditor + dead-text-detector every 3 days; open one PR
   per agent that has actionable findings.
2. Drive total token surface down measurably each cycle without
   removing important context — relocate to skills first.
3. Add @trowsey to every PR regardless of confidence band.

See `.agents/config/CONFIDENCE.md` for the rubric every PR cites.
